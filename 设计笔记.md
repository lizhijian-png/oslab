# Lab1 设计笔记：启动与串口输出

> 学号：**2024302111343**
> 本笔记用于动手实现前理清思路，也是现场验收「三题问答」的取材来源。
> 实现前请务必能**脱离本文复述**每一道思考题的论证。

---

## 0. 个性化参数总览（来自 kernel/course_sid.h）

| 参数 | 值 | 推导 |
|---|---|---|
| 学号 sid | 2024302111343 | — |
| 输出协议 | **协议 2**（整段后回显 ASCII 十进制校验和） | `sid % 3 == 2` |
| `学号 % 97` | **36 → `0x24`** | 十六进制必须 `0x` 小写、无前导零 |
| 初始内核栈 | **12 KB** | `4KB × (1 + sid%3)` = 4KB×3（`LAB1_STACK_KB=12`） |
| 节流周期 | **每满 31 字节**插 nop 循环 | `16 + sid%16` = 16+15 |
| UART 基地址 | `0x10000000` | QEMU virt 规范，全班统一 |

---

## 1. 思考题一：PC=0x1000、`-bios none`、为何链接到 0x80000000

1. **复位向量 = 0x1000**。RISC-V 上电/复位后，CPU 取第一条指令的地址固定为 0x1000。QEMU virt 在 0x1000 放了一小段**内置 boot ROM**（`memlayout.h` 注释：`00001000 -- boot ROM, provided by qemu`）。

2. **`-bios none` 的含义**：默认时 QEMU 会在 0x80000000 加载固件（OpenSBI），由固件先跑 M 态、初始化环境、再降权把控制权交给内核。`-bios none` 即**不加载任何固件**，boot ROM 直接跳到 0x80000000——那里是 `-kernel` 加载进来的**我的内核**。后果：我的内核是这台机器上跑的**第一段软件**，在 **M 态**启动，OpenSBI 原本该做的（设栈、陷阱向量、PMP、降 S 态）**全要自己做**。这正是本实验要亲手写 M→S 切换的原因。

3. **为何必须链接到 0x80000000 而非 0x0**：
   - `-kernel` 按 ELF program header 把内核加载到其**链接地址**，boot ROM 也固定跳到 0x80000000——链接地址必须与「被加载、被跳转」的地址一致。
   - 0x80000000 是 QEMU virt **RAM 起点**（`KERNBASE`），往上到 `PHYSTOP`（+128MB）才是内存。
   - 0x0 一带**没有 RAM**：0x1000 是 boot ROM、0x02000000 是 CLINT、0x0C000000 是 PLIC、0x10000000 是 UART，全是 MMIO。链接到 0x0 等于把代码放进 MMIO 区，无法运行。

---

## 2. 思考题二：entry.S 每条指令的存在理由

| 步骤 | 做什么 | 为什么 |
|---|---|---|
| ① 关中断 | `csrw mstatus, zero`（或清 MIE） | 上电时 `mtvec` 仍为 0，**没有任何陷阱处理程序**；此时来中断会跳去地址 0 崩溃。第一步先钉死为「关中断、无陷阱」，保证逐条确定性执行。 |
| ② 设初始内核栈 | `sp = stack0 + 12KB` | 进 C（`call start`）前必须有合法栈。栈放 `.bss` 静态数组 `stack0[4096*3]`（12KB），因为此刻**还没有内存分配器**，只能用编译期留好的静态空间；12KB 对「上电→start→main→printf」浅调用链绰绰有余，sp 指向栈顶、向下增长。 |
| ③ 从核 park | `mhartid != 0` → 自旋（`wfi`/`j .`） | lab1 单核。从核没被初始化、没有自己的栈和环境，若也冲进 `start()` 会互踩内存。非 0 号 hart 必须在 M 态自旋等待后续实验唤醒。 |
| ④ 设 mtvec（`.balign 4`） | `csrw mtvec, <标号>` | 陷阱向量基址**低 2 位是 MODE 标志位**，基地址必须 4 字节对齐——标号前加 `.balign 4`。不对齐则硬件**静默丢弃整次写入**（附录 B 第 1 条），此后任何陷阱都跳去地址 0。 |

---

## 3. 思考题三：进 S 态前的配置清单（M→S 切换义务）

`mret` 一条指令同时完成「降权 + 跳转」，其行为完全由以下 CSR 决定，**少配一个就崩**：

1. **PMP 授权**（最先、最关键）：
   ```c
   w_pmpaddr0(0x3fffffffffffffull);  // NAPOT 编码：覆盖全部物理地址空间
   w_pmpcfg0(0xf);                   // A=NAPOT, R=W=X=1, L=0
   ```
   新版 QEMU 规定 S/U 态的取指与访存必须落在 PMP 授权区。不配这两行，`mret` 降入 S 态的第一条取指立刻 `instruction access fault`（`-d int` 首行 `cause=1`，全程无输出）。**验收定位题：删掉这两行 → 全黑屏，cause=1。**

2. **mstatus**：`MPP = S`（决定 mret 返回 S 态）；`MIE = 0`（M 态关中断）。`mret` 会把 `MPIE→MIE`，故要确保 `MPIE=0`，降权后仍关中断（lab1 全程轮询，不使能任何中断）。

3. **mepc = main**：mret 返回后 PC 跳到 C 的 `main` 地址。

4. **中断委托**：`medeleg = 0xffff`、`mideleg = 0xffff`，把异常/中断委托给 S 态处理（否则默认留 M 态）。

5. **satp = 0**：见思考题四。

6. 最后 `mret` 一条指令完成切换。

---

## 4. 思考题四：satp 选 Bare 还是恒等映射

**决策：Bare 模式（`satp = 0`，MODE=0，地址翻译完全关闭）。**

论证：
- 本实验**尚无页表**（页表是 lab3 内容）。Bare 模式下虚拟地址 = 物理地址，跑在 0x80000000+ 低位内存 + 直接访问 0x10000000 的 UART MMIO，完全够用。
- 恒等映射（Sv39）虽能跑通，但**零收益**：要凭空建页表、映射 UART MMIO、设 satp、`sfence.vma` 刷 TLB，纯增复杂度与坑位，对 lab1 目标毫无帮助。
- xv6 正是如此：启动期 satp=0，直到创建内核页表/首个进程时才切 Sv39。

---

## 5. 思考题五：UART 16550 轮询输出协议

- **基地址** `0x10000000`（`memlayout.h` 的 `UART0`）。
- **涉及寄存器**（16550 字节偏移）：
  - `THR`（发送保持寄存器）：偏移 **0**，只写，放待发送字符；
  - `LSR`（线路状态寄存器）：偏移 **5**，只读，看状态。
- **关键状态位**：LSR 的 **bit 5 = THRE**（Transmitter Holding Register Empty，发送保持寄存器空）。
- **写条件**：**只有 THRE=1（bit5 置位）时**，THR 才是空的、能接受下一字符，此时才写，否则数据丢失/覆盖。
- 轮询逻辑：
  ```c
  while ((inb(UART0 + 5) & (1 << 5)) == 0)  /* 忙等 LSR bit5 */
      ;
  outb(UART0, c);                            /* 写 THR */
  ```
- 分层约定（附录 A「好版规范」）：本实验启动期**不设超时重试**（无时钟中断，忙等是唯一可靠行为）；`uartputc_sync` 只负责单字节硬件发射，学号个性化字符修饰放上层 `putchar`，层次分明。

---

## 6. 启动时序图（上电 → main 第一行）

```
上电/复位
  │ PC=0x1000 (复位向量)
  ▼
[QEMU 内置 boot ROM @0x1000]   -bios none: 不跑固件, 直接跳 0x80000000
  │
  ▼
_entry (entry.S, M 态)
  ├─① 关中断  mstatus.MIE=0
  ├─② 设栈    sp = stack0 + 12KB
  ├─③ 从核 park (hartid!=0 → 自旋)
  ├─④ 设 mtvec (.balign 4)
  └─⑤ call start()            ── 仍在 M 态
  ▼
start() (start.c, M 态)
  ├─ PMP 授权 (pmpaddr0/pmpcfg0)
  ├─ mstatus: MPP=S, MIE=0
  ├─ mepc = main
  ├─ medeleg/mideleg = 0xffff
  ├─ satp = 0 (Bare)
  └─ mret ──► 特权级 M→S, PC=main
  ▼
main() (S 态)  ── 串口初始化 → banner → 边界测试
```

---

## 7. 内核内存空间布局图

```
物理地址空间 (QEMU virt)
0x00000000 ┌────────────┐
0x00001000 │  boot ROM   │ 复位向量
0x02000000 │   CLINT     │
0x0C000000 │   PLIC      │
0x10000000 │  UART0      │ ← 本实验输出目标 (MMIO)
0x10001000 │  virtio     │
0x80000000 ├────────────┤ ← KERNBASE / RAM 起点 (链接地址)
           │  .text      │   entry.S → start → main
           │  .rodata    │   字符串常量
           │  .data      │   已初始化数据
           │  .bss       │   ← stack0[12KB] 在这里
           │ kernel_end  │   ← 段末 (= .)
           │  空闲 RAM   │
0x88000000 └────────────┘ ← PHYSTOP = KERNBASE + 128MB
```

---

## 8. 输出协议精确规格（协议 2）

**期望输出（`expect_banner.txt` 内容）：**

```
OSLAB1 sid=2024302111343 mod97=0x24
[chk=2276]
```

- 第一行：banner 正文，以 `\n` 结尾。
- 第二行：`[chk=<校验和>]` 为**最末段**，其后**不再有正文**（末尾不要再有换行，否则 `check_expect.py` 按 `\n` 切分后最后一段变空串而 Fail）。
- 校验和算法自定，但**必须与内核实际输出逐字节一致**。本例 `2276` = banner 正文所有字符 ASCII 值求和 `sum(ord(c) for c in line)`；也可用异或（=10）等，自洽即可。
- 十六进制字段 `0x24`：`0x` 小写、无前导零（错误示例：`0X24`、`0x024`、`24`）。

---

## 9. 关键接口设计规范（附录 A「好版」风格）

```c
void uartputc_sync(char c);
```
- 硬件地址：基地址 `0x10000000`；THR 偏移 0，LSR 偏移 5。
- 状态机：循环忙等 LSR bit5（THRE）变 1 后向 THR 写字符；启动期不设超时重试。
- 协议分层：本函数只负责单字节硬件发射；学号个性化字符修饰在上层 `putchar` 包装。
- 并发假设：启动早期仅单核单上下文调用，声明不可重入。

---

## 10. 待自填 / 待验证项（实现时补全）

- [ ] printf 边界规范：`%d` 负数、`%x` 无前导零、`0`、`INT_MAX`、空字符串、超长字符串——具体输出形态需在写 `printf.c` 前敲定。
- [ ] 节流实现：输出满 31 字节后插入 nop 循环，**引用 `COURSE_SID` 宏，严禁硬编码 31**。
- [ ] 写完 `expect_banner.txt` 后执行 `python3 check_expect.py 2024302111343` 自检形状。
- [ ] 每张图的每个字段、每条思考题，验收前都能口头复述。
